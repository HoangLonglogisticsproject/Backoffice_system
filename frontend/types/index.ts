/*
 * @bo/types — shapes shared across the whole frontend.
 *
 * Only what more than one feature speaks about. A model owned by a single
 * feature belongs in `features/<name>/domain/`, not here.
 */
export * from './access-scope';
export * from './organization.model';
export * from './owned-record.model';
